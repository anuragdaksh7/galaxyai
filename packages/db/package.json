{
  "name": "@repo/db",
  "version": "0.0.0",
  "scripts": {
    "db:generate": "npx prisma generate",
    "db:migrate:dev": "npx prisma migrate dev --skip-generate",
    "db:deploy": "npx prisma migrate deploy",
    "db:push": "npx prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^6.10.1",
    "prisma": "^6.10.1"
  },
  "exports": {
    ".": "./index.ts",
    "./client": "./client.ts"
  }
}